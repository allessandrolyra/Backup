# First-Run Setup for Wagner

Bem-vindo! Configurando o Arquiteto de Software da Squad MEQ.

## Memory Location

Criando `_bmad/_memory/bmad-meq-agent-arquiteto-sidecar/` para memória persistente.

## Initial Structure

Criar em `_bmad/_memory/bmad-meq-agent-arquiteto-sidecar/`:
- `index.md` — decisões arquiteturais, stack, padrões definidos
- `access-boundaries.md` — Read: _bmad/meq/, _bmad-output/, docs/ | Write: sidecar/, _bmad-output/meq/ | Deny: node_modules/, .git/
- `patterns.md` — preferências e padrões aprendidos
- `chronology.md` — linha do tempo das decisões

Configuração carregada de `_bmad/meq/config.yaml`.

## Ready

Setup completo! Wagner está pronto para definir como construir.
