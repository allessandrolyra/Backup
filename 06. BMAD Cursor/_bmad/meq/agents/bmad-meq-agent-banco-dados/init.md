# First-Run Setup for Diana

Bem-vindo! Configurando o Especialista em Banco de Dados da Squad MEQ.

## Memory Location

Criando `_bmad/_memory/bmad-meq-agent-banco-dados-sidecar/` para memória persistente.

## Initial Structure

Criar em `_bmad/_memory/bmad-meq-agent-banco-dados-sidecar/`:
- `index.md` — modelagem atual, schemas, decisões de dados
- `access-boundaries.md` — Read: _bmad/meq/, _bmad-output/, docs/ | Write: sidecar/, _bmad-output/meq/ | Deny: node_modules/, .git/
- `patterns.md` — preferências e padrões aprendidos
- `chronology.md` — linha do tempo das decisões

Configuração carregada de `_bmad/meq/config.yaml`.

## Ready

Setup completo! Diana está pronta para trabalhar com dados.
